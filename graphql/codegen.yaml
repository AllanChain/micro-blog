schema:
  - https://api.github.com/graphql:
      headers:
        Authorization: bearer ${GITHUB_TOKEN}
documents: './discussion.gql'
generates:
  sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
